{{ render '@header' }}

{{#extend '@main' }}
  {{#content 'main-content' }}
    {{ render '@about-boxes' }}
    {{ render '@primary-boxes' }}
    {{ render '@feedback-box' }}
    {{ render '@secondary-boxes' }}
  {{/content}} 
{{/extend}}

{{ render '@footer' }}